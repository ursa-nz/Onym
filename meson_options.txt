# SPDX-FileCopyrightText: 2026 ursa.nz <code@ursa.nz>
# SPDX-License-Identifier: GPL-3.0-or-later

option('wordnet_data_dir',
  type: 'string',
  value: '/usr/share/wordnet',
  description: 'WordNet database directory used when WNSEARCHDIR and WNHOME are unset',
)

option('onym_engine_dir',
  type: 'string',
  value: '',
  description: 'Path to an onym-engine checkout; empty means the sibling ../onym-engine, and a relative path resolves against the source root',
)

option('introspection',
  type: 'feature',
  value: 'auto',
  description: 'Build the GObject Introspection data for libonym',
)

option('app',
  type: 'feature',
  value: 'auto',
  description: 'Build the GTK4 and libadwaita application',
)

option('bundle_wordnet_overlay',
  type: 'boolean',
  value: false,
  description: 'Install the etymology overlay (data/etym.onym) into the WordNet data directory. For self-contained packages that bundle their own WordNet (Flatpak, AppImage); off for the system .deb, which uses the shared wordnet-base directory it does not own',
)
